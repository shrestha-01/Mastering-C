#include <stdio.h>

int main(){
    // naming rules 
    // First character must be an alphabet or underscore(_).
    // No commas, blanks are allowed.
    // No special symbol other than (_) allowed.
    // Varibales names are case sensitive.
    int a;
    int s_shrestha;
    int shrestha;
    int Shrestha;

    return 0;
}